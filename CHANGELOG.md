# Changelog — greetings

What changed for someone making or opening a greeting card. It deploys from `main`, so
landing *is* releasing: sections are months, each entry dated by its landing. Per
`CroftC/.claude/CHANGELOGS.md`, the branch that changes something a user runs adds its entry
here before it lands. Started 2026-08-29; earlier history is in `git log`.

## 2026-08

- 2026-08-26 Accessibility and mobile-fit gates, and the serious contrast defect they found
  on their first run — fixed, and `@axe-core/playwright` pinned to the canonical 4.12.1 in
  the lockfile so the scan grades the same DOM everywhere.
